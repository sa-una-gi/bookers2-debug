<% chats.each do |c| %>

  <% if c.user_id == user.id %>
    <div class="your-chat" id="chat-<%= @room.id %>"><%= c.message %></div>
  <% else %>
    <div class="my-chat"><%= c.message %></div>
  <% end %>
<% end %>