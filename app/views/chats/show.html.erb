<h1><%= @user.name %>さんとのチャット</h1>

<div class="col-xs-4">

<div id="chat-room-<%= @room.id %>">
  <%= render 'chats', chats: @chats,user: @user %>
</div>

<%= form_for(@chat,url: "/users/#{@user.id}/room/#{@room.id}",remote: true) do |f| %>

  <%= f.text_field :message %>

<% end %>

</div>


